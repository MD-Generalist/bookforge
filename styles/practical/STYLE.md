# Practical — 실용·활용서

## 정체성
따라 하면 결과가 나오는 책. 지면은 "회차 헤더 → 결과물 → 재료(프롬프트·코드) → 단계"라는 한 벌의 반복 단위로만 굴러가고, 독자는 목차에서 본 번호를 지면에서 그대로 다시 만난다.
색은 브랜드 1색 + 단계 1색 + 경고 1색으로 역할이 고정되며, 장식이 아니라 위치 표지로만 쓰인다.
서술은 명조로 낮게 깔고, 조작·라벨·수치는 고딕으로 세워 "읽는 글"과 "하는 글"을 서체로 분리한다.

## 판형·판면
- **기본 판형** 182 × 251 mm (46배판 변형, 견본2 실측 515.90 × 711.50 pt). 화면 캡처·결과 이미지가 있는 활용서의 기본값.
- **컴팩트 변형** 153 × 225 mm (신국판, 견본1 실측 433.70 × 637.79 pt). 이미지가 적은 항목형·용어집형에만 허용.
- **재단 여백(bleed)** 4변 3 mm. 헤더 밴드·도비라 색면·풀블리드 이미지는 재단선 밖 3 mm까지 연장. 그 외 요소는 금지.
- **마진(182×251 기준)** 위 15 mm / 아래 21.5 mm / 안쪽 26.5 mm / 바깥쪽 16.5 mm → **판면 139 × 214.5 mm**
- **마진(153×225 기준)** 위 13 mm / 아래 19 mm / 안쪽 21 mm / 바깥쪽 13.4 mm → 판면 118.6 × 193 mm
- **기준선 격자** 19 pt (6.703 mm). 판면 높이 214.5 mm = 정확히 32행. 본문·소제목·박스는 모두 이 격자에 스냅.
- **그리드** 1단(판면 폭 전체 139 mm)이 정본. 박스 내부와 이미지 그리드에 한해 2단 허용 — 컬럼 63.6 mm, 거터 11.8 mm(4.2 mm 아님 주의: 실측 11.9 pt = 4.2 mm이므로 **거터 4.2 mm**, 컬럼 67.4 mm).
- **좌우 미러링** 안쪽/바깥쪽 마진은 펼침면 기준으로 반전한다(짝수 쪽 = 바깥쪽이 왼쪽).

## 폰트 스택
표기: `1순위 → 대체(동봉 폰트)`. 대체 사용 시 pt는 괄호 값으로 교체한다.

| 역할 | 폰트 | 웨이트 | pt | 행간 | 자간 |
|---|---|---|---|---|---|
| 서술 본문 | KoPubWorld바탕 PL → Noto Serif KR | Light(Regular) | 9.8 (9.5) | 19 pt / 194% | −1% |
| 절차·박스 본문 | Pretendard | Regular | 8.9 | 15 pt / 169% | −1% |
| 항목형 본문(변형B) | Pretendard | Light | 9.0 | 14.5 pt / 161% | −1% |
| 소제목 H3 | Pretendard | Bold | 11.3 | 16 pt / 142% | −2% |
| 소제목 번호 | Pretendard | ExtraBold | 13.5 | — | −2% |
| 단계 마커 | Pretendard | ExtraBold | 13.8 | 본문 격자 | −2% |
| 유닛 표제(장제목) | Gmarket Sans → Pretendard | Bold | 20.9 | 32 pt / 153% | −3% |
| 회차 대형 숫자 | Pretendard | Black | 55 | — | −4% |
| 도비라 장번호 | Pretendard | Black | 150 | — | −5% |
| 표지 주제목 | Pretendard | Black | 78 | 82 pt / 105% | −4% |
| 표지 보조제목 | Pretendard | Black | 40 | — | −4% |
| 정의 박스 리드 | Pretendard → Paperlogy | SemiBold | 9.5 | 14.5 pt / 153% | −2% |
| 캡션 | Pretendard | Light | 7.5 | 9.5 pt / 127% | 0% |
| 출처 | Pretendard | Light | 7.0 | 9.5 pt / 136% | 0% |
| 라벨·탭 | Pretendard | Bold | 6.9 | — | 0% |
| 러닝푸터 | Pretendard | Bold + Regular | 7.5 | — | 0% |
| 쪽번호 | Pretendard | Bold | 8.0 | — | 0% |
| 표 본문 | Pretendard | Regular | 8.4 | 13 pt / 155% | −1% |

- 지면 1쪽에 등장하는 **서체 패밀리는 최대 3종**(서술 명조 1 + Pretendard 1 + 디스플레이 1).
- 숫자는 전부 tabular figure. 회차·쪽번호·표 안 수치에 프로포셔널 금지.
- 대체 폰트 사용 시 로그에 `font_fallback: <role>=<font>`를 남기고 표지·도비라의 자간을 −1%p 추가 조인다.

## 컬러 토큰
기본 팔레트 1세트 (Teal, 견본2 실측)

| 토큰 | hex | 역할 |
|---|---|---|
| `ink` | `#231F20` | 본문·표제 기본 글자색 |
| `ink-mute` | `#404041` | 부가 설명·표 캡션 |
| `gray` | `#6D6E71` | 출처·크레딧 |
| `rule` | `#D9DCDE` | 카드 테두리·표 괘선 |
| `paper` | `#FFFFFF` | 지면·카드 바탕 |
| `brand` | `#2DA082` | 헤더 밴드·탭·러닝푸터·소제목 |
| `brand-deep` | `#1F7A63` | 밝은 바탕 위 브랜드색 글자 |
| `brand-tint` | `#D6E5DE` | 헤더 밴드 그라데이션 시작 |
| `brand-pale` | `#EAF4F0` | 정의 박스 바탕·목차 필 |
| `highlight` | `#BDD756` | 브랜드 색면 위 카테고리 라벨 |
| `step` | `#F47932` | 단계 번호·AI 발화 태그 |
| `step-pale` | `#FDEDE3` | 주의 박스 바탕 |
| `alert` | `#ED1C24` | 경고 라벨·주석 테두리 |
| `info` | `#0080AB` | 프롬프트·코드 카드 탭 |

**주제별 브랜드색 교체 규칙**
- 교체 대상은 `brand / brand-deep / brand-tint / brand-pale` **4개뿐**. `step·alert·info·highlight·ink 계열`은 절대 교체하지 않는다.
- 검증된 대체 세트 — Blue: `#007EBD / #005E8D / #8CC4EB / #E6F1FA` (견본1 1장) · Amber: `#F6B85E / #C98A34 / #F3D482 / #FEF6EA` (견본1 2장).
- `brand`의 흰색 대비가 3.0:1 미만이면 글자에는 반드시 `brand-deep`을 쓰고 `brand`는 색면 전용으로 강등한다.
- 부(部)마다 팔레트를 바꾸는 것은 허용, 유닛 단위 교체는 금지.

## 표지 문법
- 캔버스 182 × 251 mm + bleed 3 mm. 벡터 타이포만으로 성립해야 하며, 이미지 자산이 0개여도 완성되어야 한다.
- **색면 분할(상→하)** 브랜드 색면 0–118 mm(47%) / 결과 스트립 밴드 118–183 mm(26%) / 여백 밴드 183–251 mm.
- 브랜드 색면 위에 흰색 라운드 패널(반경 12 mm)을 좌우 −6 mm 오버행으로 얹고 그 안에 타이포를 앉힌다.
- **요소 순서** ① 시리즈 필 ② 주제목 ③ 보조제목 ④ 부제 리버스 필 ⑤ 저자 ⑥ 결과 스트립 ⑦ 발행처.
  - 시리즈 필: 캡슐, 높이 9 mm, `highlight` 필, Pretendard Bold 13 pt / `ink`. 좌측 x = 22 mm, 상단 y = 22 mm.
  - 주제목: Black 78 pt, 좌측 정렬 x = 22 mm, 최대 2행. 보조제목은 0.5배(40 pt)로 주제목 마지막 행 베이스라인에 정렬.
  - 부제 필: `brand` 필, 높이 16 mm, 라운드 5 mm, Pretendard Bold 24 pt / `paper`.
  - 저자: Bold 10 pt(이름) + Regular 10 pt(“지음”), 우측 정렬, 부제 필 오른쪽 아래 6 mm.
  - 결과 스트립: 4컷 균등, 컷 폭 38 mm, 거터 6 mm, 밴드 상하 패딩 8 mm. 컷 수는 4 또는 0만 허용.
  - 발행처: 하단 12 mm, 우측 정렬, Bold 9 pt.
- **자산 0컷 대안(견본1형)** 파스텔 단색 배경 + 리본 배너 부제(높이 8 mm, 좌우 화살 꼬리) + 중앙 정렬 주제목, 주제목 중 **한 단어만** `brand` 색 1.6배 확대, 하단 중앙 발행처 락업. 이 대안이 이미지 없는 책의 기본값.
- 표지 서체 2종 이내, 색 3종 이내.

## 목차 문법
- 파트당 1쪽. 파트가 1개뿐이면 목차도 1쪽.
- 상단에 유닛 오프너와 **동일 규격의 헤더 밴드**(높이 56 mm)를 쓰되 우하단 모서리만 반경 12 mm로 깎는다.
- PART 배지: `brand` 라운드 사각 23 × 46 mm, 반경 3 mm, 좌측 x = 27 mm, 상단 y = 41.6 mm. 내부는 “PART” Bold 20 pt / `highlight` → 흰 룰(폭 16 mm, 0.6 pt) → 숫자 Black 46 pt / `paper`.
- 파트 제목: Gmarket Sans Bold 20.9 pt / `ink`, 배지 오른쪽 6 mm, 배지 상단에 정렬.
- 항목 행(행간 20 pt): `[필 라벨] [제목] [점 리더] [쪽번호]`
  - 필 라벨: `brand-pale` 필, 높이 4.7 mm, 폭 자동, Bold 6.9 pt / `brand-deep`. 형식 `유닛명 | 01`.
  - 제목: Pretendard Regular 9.5 pt / `ink`, 라벨 오른쪽 3 mm.
  - 점 리더: 0.5 pt 원점, 간격 2 pt, `rule` 색.
  - 쪽번호: Medium 10 pt / `ink`, 우측 정렬(판면 오른쪽 끝).
- **2단 변형(견본1형, 항목 40개 초과 시)** 2단 × 컬럼 63.6 mm, 거터 11.8 mm, 항목 Light 9 pt, 행간 16 pt, 3자리 번호 좌측 정렬 후 제목 4 mm 들여쓰기. 단 경계에 0.3 pt `brand` 세로 룰.

## 장 도비라 문법
- 풀블리드 `brand` 단색 1쪽. 좌·우 어느 쪽이든 무방하되 항상 홀수 쪽 앞에 온다. 쪽번호·러닝푸터 없음.
- 요소와 좌표(182 × 251 기준, 판면 폭 139 mm 안에서 중앙 정렬):
  1. 파트 제목 — Black 30 pt / `brand-tint`, y 22 → 52 mm, 최대 2행, 중앙 정렬.
  2. 상단 수평 룰 — 0.5 pt `paper`, 판면 폭 전체, y = 60.5 mm.
  3. 상단 수직 룰 — 0.5 pt `paper`, 판면 중심 x, y 60.5 → 94 mm, 하단에 폭 5 mm 캡 틱.
  4. 초대형 장번호 — Black 150 pt / `brand-tint`, 중앙 정렬, y 98 → 194 mm.
  5. 하단 수직 룰 — 상단과 대칭, y 165 → 199 mm, 상단에 폭 5 mm 캡 틱.
  6. 하단 수평 룰 — 0.5 pt `paper`, y = 199 mm.
  7. 장 요약문 — Pretendard Regular 9.5 pt / 14 pt / `paper`, 중앙 정렬, 4–5행, y 207 → 229 mm. 첫 구절(장 이름)만 Bold.
- 장번호는 항상 1자리 또는 2자리. 3자리 이상이면 도비라를 쓰지 않는다.

## 지면 템플릿
반복 단위 = **유닛(활용/실습 1회차)**. 유닛은 반드시 짝수 쪽 배수(2·4·6쪽)로 끝난다.

### A. 유닛 오프너 (유닛 첫 쪽, 판면 무시하고 헤더가 풀블리드)
1. **헤더 밴드** — 상단 y 0 → 56 mm, 좌우 재단선 밖 3 mm까지.
   - 좌측 브랜드 블록: x 0 → 42 mm, `brand` 단색.
     - 시리즈 필: 흰 캡슐 17.7 × 4.5 mm, 중심 x = 29.5 mm, y 17.0 → 21.5 mm, 텍스트 Bold 4.8 pt / `brand`.
     - 카테고리 라벨: Bold 12.9 pt / `highlight`, 중심 x = 29.5 mm, 글자 상단 y = 22.0 mm.
     - 구분 룰: 0.6 pt `paper`, 폭 16.3 mm, 중심 x = 29.5 mm, y = 31.4 mm.
     - 회차 숫자: Black 55 pt / `paper`, 중심 x = 29.5 mm, **글자 하단이 밴드 하단(56 mm)에 정확히 맞음**.
   - 우측 제목 밴드: x 45 mm → 재단선 밖, 가로 그라데이션 `brand-tint` → `brand-pale`(0°).
     - 표제: 20.9 pt Bold / `ink`, x = 48 mm, 최대 2행, **블록 하단이 밴드 하단에서 4 mm 위**.
   - 좌측 블록과 제목 밴드 사이 간격 3 mm(흰 틈).
2. **결과 이미지 블록** — 밴드 하단에서 20 mm 아래 시작.
   - 탭 라벨 “결과 이미지”: `brand` 필, 18.3 × 4.7 mm, 반경 1 mm, Bold 6.9 pt / `paper`, 판면 왼쪽 끝에 맞춤.
   - 이미지: 1컷(판면 폭 139 mm) 또는 2컷(67.4 mm × 2, 거터 4.2 mm). 라벨 아래 3 mm.
3. **프롬프트/코드 카드**
   - 흰 필, `rule` 0.5 pt 스트로크, 반경 3 mm, 폭 = 판면 폭, 내부 패딩 3.6 mm.
   - 우상단 탭 라벨: `info` 필, 13 × 4.5 mm, 반경 1 mm, Bold 6.9 pt / `paper`. **탭 세로 중심이 카드 상단선과 일치**, 탭 오른쪽 = 카드 오른쪽 − 3.2 mm.
   - 카드 첫 줄 앞에 프롬프트 글리프(`brand` 라운드 사각 4.7 × 2.5 mm, 흰 점 3개).
   - 본문 Pretendard Regular 8.9 pt / 15 pt. 항목이 6개를 넘으면 카드 내부를 2단(67.4 mm × 2, 거터 4.2 mm)으로 쪼갠다.
   - 항목 라벨(`[목적]`, `[배경]` 등): Bold 7.9 pt / `brand-deep`, 항목 위 3 mm.
4. 러닝푸터.

### B. 진행 쪽 (2쪽째 이후)
1. **서술 단락** — 서술 본문 9.8 pt / 19 pt, 첫 줄 들여쓰기 없음, 단락 간격 0(격자 1행 = 19 pt로 대체).
2. **단계 문단** — 마커 `step` ExtraBold 13.8 pt를 **판면 왼쪽 끝에 내어쓰기**, 본문은 8.4 mm 들여쓰기. 마커는 `01`부터 2자리 고정.
3. **소제목 H3** — Bold 11.3 pt / `brand`, 판면 왼쪽 + 1.5 mm. 앞에 번호가 붙으면 ExtraBold 13.5 pt / `brand`. 위 여백 2행(38 pt), 아래 1행(19 pt).
4. **대화 카드(나/AI)** — 흰 필 + `rule` 0.5 pt, 반경 3 mm, 폭 = 판면 폭, 패딩 3.6 mm. 화자 태그는 말풍선 5.4 × 3.0 mm(나 = `brand`, AI = `step`), 텍스트 Bold 5.7 pt / `paper`. 태그 아래 2 mm에 발화문 8.9 pt.
5. **이미지 + 캡션** — 아래 「캡션·표·그림 규약」.
6. 러닝푸터.

### C. 항목형 변형 (용어·레퍼런스 유닛, 견본1 계열 — 153 × 225에서만)
1. 러닝헤드 박스 라벨: y 10.0 → 14.3 mm. 흰 필 + `brand` 0.3 pt 스트로크 박스(폭 33.7 mm, Light 7.5 pt / `ink`) + 이어 붙은 `brand` 단색 박스(폭 10.8 mm, Regular 7.5 pt / `paper`).
2. 좌측 세로 룰: x = 17.0 mm, 0.3 pt `brand`, **위아래 풀블리드 관통**.
3. 표제 블록: 3자리 회차 숫자(Black 30 pt, 앞의 0은 `brand-tint`, 유효 숫자는 `brand`) x = 21.0 mm, y 19.7 → 33.1 mm → 그 오른쪽 x = 43.0 mm에 표제 Bold 24 pt / `ink`, 아래 영문 부제 Light 9 pt / `ink`.
4. 정의 박스: `brand-pale` 필 + `brand` 0.28 pt, x 15.1 → 139.0 mm(**판면보다 좌측으로 5.9 mm 내밀림**), 상단 y = 42.0 mm. 리드 SemiBold 9.5 pt / `brand` + 불릿 Light 9 pt / 14.5 pt.
5. 소제목: 원형 불릿(지름 2.2 mm, `brand`)을 x = 15.9 mm에 두어 **세로 룰 위에 걸치게** 하고, 텍스트 SemiBold 9.5 pt / `brand`를 x = 21.0 mm에 둔다.
6. 본문: Light 9 pt / 14.5 pt, x 21.0 → 139.6 mm(측정 폭 118.6 mm). 소제목 다음 1.2 mm, 다음 소제목 앞 6.1 mm.
7. 쪽번호: 흰 필 + `brand` 0.3 pt 박스 4.9 × 4.2 mm, 우하단 x 140.4 → 145.3 mm, y 212.0 → 216.3 mm, 숫자 Bold 8 pt / `ink` 중앙.

## 박스·콜아웃·라벨 위계
4단계만 존재하며, 이 순서로만 강도가 올라간다.
1. **L1 정의·요약 박스** — `brand-pale` 필 + `brand` 0.28 pt, 반경 0, 판면보다 좌우 각 4.5 mm 넓게 내밀림. 리드 SemiBold 9.5 pt / `brand` 1행 + 불릿 본문. 유닛당 최대 1개, 항상 첫 본문 앞.
2. **L2 재료 카드(프롬프트·코드·대화)** — 흰 필 + `rule` 0.5 pt, 반경 3 mm, 탭 라벨 필수(`info` 또는 `brand`). 유닛당 개수 제한 없음.
3. **L3 인라인 팁·노트** — 배경 없음. 왼쪽에 2 pt `brand` 세로 룰, 본문 5 mm 들여쓰기, Regular 8.9 pt / 15 pt. 라벨은 Bold 6.9 pt / `brand-deep` 인라인.
4. **L4 주의·경고** — `step-pale` 필 + `step` 0.5 pt, 반경 3 mm. 라벨 필 `alert`(높이 4.5 mm, Bold 6.9 pt / `paper`)를 박스 좌상단 안쪽 3 mm에 배치. 쪽당 1개.

라벨 도형은 3종뿐 — **탭**(반경 1 mm, 높이 4.5–4.7 mm, 박스 경계에 걸침) · **캡슐 필**(완전 라운드, 높이 4.5 mm, 문장 안/목차) · **번호 배지**(정사각 반경 1 mm, 5 × 5 mm, 이미지 주석용).

## 캡션·표·그림 규약
- **캡션** `▲ ` + Light 7.5 pt / `ink`, 이미지 하단 2.5 mm. 이미지 1컷이면 좌측 정렬, 그리드면 각 컷 하단 좌측 정렬. 마침표 없음.
- **출처** Light 7.0 pt / `gray`, 캡션 오른쪽 끝에 우측 정렬 또는 캡션 다음 줄. 형식 `출처 : <이름>`.
- **표** 세로 괘선 없음. 헤더행 = `brand-pale` 필 + 하단 `brand` 0.5 pt, 텍스트 Bold 8.4 pt / `brand-deep`. 본문행 하단 0.3 pt `rule`. 셀 패딩 상하 1.8 mm / 좌우 2.5 mm. 표 폭 = 판면 폭, 마지막 행 아래 0.5 pt `rule`.
- **그림 번호를 붙이지 않는다.** 본문에서는 “아래 그림”이 아니라 캡션 문구로 지시한다.
- **UI 캡처** 직각 모서리 + `rule` 0.5 pt 테두리. **결과물 이미지** 반경 2 mm 마스크, 테두리 없음.
- 이미지 그리드는 2 × 2 고정 — 컷 폭 67.4 mm, 거터 4.2 mm, 행 간격 6 mm(캡션 포함).

## 러닝 시스템
- **러닝푸터(모든 본문 쪽 필수)** 베이스라인 = 재단선 아래에서 10 mm.
  - 짝수 쪽(왼쪽): `[쪽번호]`(x = 바깥쪽 마진 16.5 mm) → 9 mm 간격 → `[유닛 라벨]` Bold 7.5 pt / `brand` → `[유닛 제목]` Regular 7.5 pt / `ink`. 좌측 정렬.
  - 홀수 쪽(오른쪽): 같은 요소를 좌우 반전해 우측 정렬(`유닛 라벨 → 유닛 제목 → 쪽번호`).
  - 쪽번호 Bold 8 pt / `brand`, tabular.
  - 유닛 제목이 40 mm를 넘으면 말줄임 없이 앞 어절 단위로 자른다.
- **러닝헤드** 기본 판형에서는 쓰지 않는다. 항목형 변형(C)에서만 상단 박스 라벨을 쓴다.
- **생략 대상** 표지·판권·목차·파트 도비라·장 도비라·전면 이미지 쪽 — 러닝푸터와 쪽번호 모두 없음(쪽수 카운트는 유지).

## 이미지 정책
- 해상도: 인쇄 300 dpi / 전자책 150 dpi 이상. 미달분은 배치 폭을 줄여 dpi를 맞추고, 확대 보간 금지.
- 배치 폭은 **판면 폭(139 mm) · 1/2 컬럼(67.4 mm) · 풀블리드(188 × 257 mm)** 세 가지만 허용. 임의 폭 금지.
- 풀블리드는 유닛당 1쪽까지, 그 쪽에는 본문 텍스트를 얹지 않는다.
- 이미지 위 주석이 필요하면 **번호 배지(5 × 5 mm, `alert` 필, Bold 6.9 pt / `paper`)를 이미지 모서리 기준 3 mm 안쪽 격자 위치에 놓고**, 설명은 이미지 아래 번호 목록으로 뺀다.
- 이미지 상하 여백은 격자 1행(19 pt) 단위로만 잡는다.

## 금지 사항
1. **이미지 위에 좌표 의존 지시선·화살표·말풍선 주석을 합성하지 않는다.** 콘텐츠에 따라 위치가 달라져 자동 조판이 불가능하므로 반드시 번호 배지 + 하단 목록으로 대체한다.
2. **토큰 14종 밖의 색을 쓰지 않는다.** 그라데이션은 유닛 오프너 헤더의 제목 밴드 1곳에만 허용하며, 그 외 모든 면은 단색이다.
3. **박스를 중첩하지 않는다.** 또한 L1–L4 박스가 3개 연속으로 오면 사이에 서술 단락을 최소 2행 넣는다.
4. **밑줄·이탤릭·전각 따옴표·자간 확장 강조를 쓰지 않는다.** 강조 수단은 SemiBold/Bold와 `brand`·`step` 색 두 가지뿐이다.
5. **한 지면에 서체 패밀리 3종, 위계 5단을 넘기지 않는다.** 소제목은 H3까지만 존재하며 H4 이하를 만들지 않는다.

## 레퍼런스 출처
- 견본1 — NIA 한국지능정보사회진흥원, 『2025년 AI 동향과 이슈로 살펴보는 AI 시대에 꼭 알아야 할 핵심 용어』. 원본 `/mnt/d/PDF스킬/ex/(배포용)_2025_AI_동향과_이슈로_살펴보는_AI_시대에_꼭_알아야_할_핵심용어_단면.pdf`. 실측 페이지 1(표지)·2, 4(목차)·7(도비라)·9·29·79·125. 판면 433.70 × 637.79 pt, 본문 Pretendard-Light 9 pt / 14.5 pt, 표제 Paperlogy-7Bold 24 pt, 브랜드 `#007EBD`·`#F6B85E`.
- 견본2 — 공냥이 지음, 『이게 되네? GPT 이미지 2 미친 활용법 61제』, 골든래빗. 원본 `/mnt/d/PDF스킬/ex/이게 되네 GPT 이미지 2 미친 활용법 61제_본문_최종_저자증정.pdf`. 실측 페이지 1(표지)·4(이 책을 보는 방법)·9(파트 목차)·20·21·68·69·105. 판면 515.90 × 711.50 pt, 본문 KoPubWorldBatangPL 9.8 pt / 19 pt, 표제 GmarketSansBold 20.9 pt, 브랜드 `#2DA082`, 단계 `#F47932`.
- 판형·마진·행수 관행 — 「편집 디자인할 때 이건 반드시 알아야 한다」 brunch.co.kr/@sweetannie/231 (본문 9.5–10.5 pt, 21–26행, 텍스트 프레임 85–105 mm), 북토리 인쇄가이드 booktory.com/make-guide/pg1-1.asp (신국판 153 × 224 mm).
- 표지 위계·서체 수 관행 — 「표지 디자인의 실무 용어와 텍스트 배치에 관하여」 brunch.co.kr/@yirangok/2, 「좋은 책 표지 디자인은 어떻게 해야 할까?」 festbook.co.kr (제목 대비 부제·저자 축소, 실용/IT는 고딕 계열, 서체 2종 이내).
- 실습·따라하기 구성 관행 — 길벗 「무작정 따라하기」·한빛미디어 실용서 시리즈 소개 페이지 gilbut.co.kr, hanbit.co.kr (간단 실습 → 단계 번호 → 고급 활용 순의 회차 반복 구성).
